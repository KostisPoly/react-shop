import React from 'react'
import './signin.scss'
import Login from '../auth/Login'

const Signin = (props) => {
    return (
        <div className="sign-in">
            <Login />
        </div>
    )
}

export default Signin;